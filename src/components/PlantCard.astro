---
const { name, img, link } = Astro.props;
---
<a href={link} class="block bg-white border border-gray-300 rounded-lg p-4 text-center">
    <img src={img} alt={name} class="w-full rounded-lg mb-2">
    <p>{name}</p>
</a>
