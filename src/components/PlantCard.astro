---
const { name, img, alt } = Astro.props;
---

<section
  class="flex items-center flex-col bg-white border border-gray-300 rounded-lg p-4 text-center"
>
  <img src={img} alt={name} class="w-full rounded-lg mb-4 md:w-10/12" />
  <p>{name}</p>
</section>
